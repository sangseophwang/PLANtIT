version: "3"

services:
  backend:
    container_name: backend
    image: jhj967878/backend:django
    volumes:
      - ./server:/project/server
    ports:
      - 8000:8000
    working_dir: /project/server
    command: python3 manage.py runserver 0.0.0.0:8000